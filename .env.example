# Public — safe to expose to the browser (Supabase anon key is designed for this,
# access is controlled by row-level security policies in Supabase).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only — used by api/generate.js, never prefix with VITE_.
GEMINI_API_KEY=
